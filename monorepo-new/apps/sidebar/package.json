{
  "name": "@g-assistant/sidebar",
  "version": "1.0.0",
  "description": "G-Assistant Sidebar for Google Sheets",
  "main": "src/index.ts",
  "scripts": {
    "build": "node build.js",
    "deploy": "npm run build && clasp push",
    "dev": "npm run build -- --watch",
    "test": "jest",
    "lint": "eslint src --ext .ts,.js"
  },
  "dependencies": {
    "@g-assistant/core-services": "workspace:*",
    "@g-assistant/shared-types": "workspace:*"
  },
  "devDependencies": {
    "@google/clasp": "^2.4.2",
    "esbuild": "^0.19.0",
    "fs-extra": "^11.0.0",
    "typescript": "^5.0.0",
    "jest": "^29.0.0",
    "eslint": "^8.0.0"
  }
}