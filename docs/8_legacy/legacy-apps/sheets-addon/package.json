{
  "name": "@g-assistant/sheets-addon",
  "version": "1.0.0",
  "description": "G-Assistant Sheets Add-on",
  "main": "src/server/main.gs",
  "scripts": {
    "build": "node build.js",
    "deploy": "npm run build && clasp push",
    "dev": "npm run build -- --watch",
    "test": "jest",
    "lint": "eslint src --ext .ts,.js",
    "push": "clasp push",
    "pull": "clasp pull"
  },
  "dependencies": {
    "@g-assistant/core-logic": "workspace:*"
  },
  "devDependencies": {
    "@google/clasp": "^2.4.2",
    "@types/google-apps-script": "^1.0.83",
    "fs-extra": "^11.0.0",
    "jest": "^29.0.0",
    "eslint": "^8.0.0"
  }
}