// Nexus Design System - Main Export
export { nexusColors, nexusCSSVars, nexusGradients } from './colors';
export { nexusTheme, nexusDarkTheme, nexusComponents } from './theme';

// Usage Examples:
/*
// Import colors
import { nexusColors } from '@nexus/ui';
const primaryColor = nexusColors.primary[500];

// Import theme
import { nexusTheme } from '@nexus/ui';
const spacing = nexusTheme.spacing.md;

// Import CSS (in your main CSS file)
@import '@nexus/ui/styles';

// Use CSS classes
<div className="nexus-gradient-primary nexus-text-gradient">
  Nexus AI
</div>
*/